# The "Sponsor" button on this repository's page. Only `custom` is set:
# this project takes no platform-based sponsorship, and an unset key
# renders as nothing rather than as a broken link.
custom: https://www.tbank.ru/cf/2qxNvGa3fSX
